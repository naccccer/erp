# Reviewer Rules

Canonical reviewer policy lives in:
- `.cursor/agents/erp-reviewer.md`

This file is intentionally a pointer to avoid policy drift.

Use the canonical file for:
- required request/response contract keys
- approval/rejection criteria
- deterministic output format
